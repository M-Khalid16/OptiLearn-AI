# OptiLearn AI local configuration template

# Copy this file to .env only if your local tooling loads environment variables.
# Never commit a real API key.

OPENAI_API_KEY=
OPENAI_MODEL=
OPTILEARN_DEMO_MODE=false

# Copy to .env only for local development if your shell tooling loads .env files.
# Do not commit real API keys.

OPENAI_API_KEY=""
OPENAI_MODEL="ADD_VERIFIED_MODEL_NAME"
OPTILEARN_DEMO_MODE="false"

